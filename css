/* --- TECH 101 FOUNDATION BRAND STYLES --- */

/* 1. Import Fonts: Bitter (Headers) and Montserrat (Body) */
@import url('https://fonts.googleapis.com/css2?family=Bitter:wght@700&family=Montserrat:wght@400;600&display=swap');

:root {
    /* 2. Color Palette Definition */
    --forest-green: #2E5936;
    --sage-green: #8FBC8F;
    --charcoal: #333333;
    --parchment: #F9F8F2;
    --white: #ffffff;
}

/* 3. Global Resets & Accessibility Settings */
* {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
}

body {
    background-color: var(--parchment); /* Warm background to reduce glare */
    color: var(--charcoal); /* High contrast but not harsh black */
    font-family: 'Montserrat', Arial, sans-serif;
    line-height: 1.6;
    /* Setting base size larger for readability (approx 20px) */
    font-size: 1.25rem; 
}

h1, h2, h3 {
    font-family: 'Bitter', Georgia, serif;
    color: var(--forest-green);
    margin-bottom: 1rem;
    line-height: 1.3;
}

h1 { font-size: 2.5rem; }
h2 { font-size: 2rem; }
h3 { font-size: 1.5rem; }

a {
    text-decoration: none;
    color: var(--forest-green);
    transition: color 0.3s ease;
}

a:hover {
    color: var(--sage-green);
}

/* --- LAYOUT UTILITIES --- */
.container {
    width: 90%;
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
}

/* --- HEADER & NAVIGATION --- */
header {
    background-color: var(--white);
    padding: 1rem 0;
    border-bottom: 5px solid var(--sage-green);
}

.nav-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
}

.logo-area {
    display: flex;
    align-items: center;
    gap: 15px;
}

.logo-img {
    height: 80px; /* Adjust based on your actual logo file */
    width: auto;
}

.logo-text {
    font-size: 1.8rem;
    font-weight: 700;
    font-family: 'Bitter', serif;
    color: var(--forest-green);
}

nav ul {
    list-style: none;
    display: flex;
    gap: 30px;
}

nav a {
    font-weight: 600;
    font-size: 1.1rem;
}

/* --- HERO SECTION --- */
.hero {
    background-color: var(--forest-green);
    color: var(--parchment);
    padding: 4rem 0;
    text-align: center;
}

.hero h1 {
    color: var(--sage-green); /* Contrast against dark green bg */
    margin-bottom: 1rem;
}

.hero p {
    font-size: 1.5rem;
    max-width: 800px;
    margin: 0 auto 2rem auto;
}

/* Primary Button Styling */
.btn {
    display: inline-block;
    background-color: var(--sage-green);
    color: var(--forest-green);
    padding: 15px 30px;
    border-radius: 8px; /* Soft corners */
    font-weight: 700;
    font-size: 1.2rem;
    border: 2px solid var(--sage-green);
}

.btn:hover {
    background-color: var(--parchment);
    color: var(--forest-green);
    border-color: var(--parchment);
}

/* --- CONTENT SECTIONS --- */
section {
    padding: 4rem 0;
}

.mission-vision {
    background-color: var(--white);
    border-top: 1px solid #ddd;
    border-bottom: 1px solid #ddd;
}

.grid-2 {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
}

.card {
    background: var(--parchment);
    padding: 2rem;
    border-radius: 10px;
    border-left: 8px solid var(--forest-green);
}

/* --- FOOTER --- */
footer {
    background-color: var(--charcoal);
    color: var(--parchment);
    padding: 3rem 0;
    text-align: center;
    margin-top: auto;
}

footer p {
    font-size: 1rem;
}

footer a {
    color: var(--sage-green);
}

/* --- RESPONSIVE MOBILE --- */
@media (max-width: 768px) {
    .nav-container {
        flex-direction: column;
        text-align: center;
    }
    
    nav ul {
        margin-top: 20px;
        flex-direction: column;
        gap: 15px;
    }
    
    .grid-2 {
        grid-template-columns: 1fr;
        gap: 2rem;
    }
}
